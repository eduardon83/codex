// Tutorial system removed. The `?` button now opens a static help modal
// sourced from the `app_content` table (category: 'help'). This file is kept
// as a thin re-export so existing import sites continue to work.
export { default } from '@/components/HelpModal';
